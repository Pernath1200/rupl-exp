# Pisanie repair queue — full-sentence type items → cloze

**Locked by James, 2026-08-06 (dropdown session):**

- **The line: typed-whole answers may be at most 3 words.** 4+ words = convert to a cloze.
- **The blank = the unit's teaching point** (the pattern word the unit exists to teach: gdybym/że/którym/mógłby/robiłeś…). If an item contains no teaching-point word (pure recycled scaffold), blank the most load-bearing form and log it in the item's note.
- Format: `{"mode": "cloze", "frame": "Gdyby mama ___ czas, zrobiłaby obiad.", "answer": "miała", "prompt_en": …, "accepts": […]}` — engine support shipped (practice-grammar cloze branch). `frame` must contain exactly one `___`. `accepts` = genuine answer variants only.
- Prompts (`prompt_en`) stay as they are unless the blank makes them misleading.
- **Refinement (James, 2026-08-06, second smoke report):** the word cap alone is not the rule. **Frozen chunks taught whole** (Co słychać?, Nic nowego., Wszystko w porządku.) and **pattern phrases** (Było super., w nowym domu) may be typed whole at ≤3 words; **COMPOSED sentences are cloze regardless of length** — anything built from parts (chunk + bolted-on phrase like "Jak było w pracy?", or a real conjugated question like "Co robiłaś wczoraj?"). While repairing a queued pack, ALSO audit its ≤3-word type items against this test and cloze the composed ones (they are not listed below — the sweep was word-count-based and cannot see composition). Worked example: a2_smalltalk items 8-11, converted by hand 2026-08-06 ("Jak było ___?" → w weekend / w pracy; "Co ___ wczoraj?" → robiłeś / robiłaś).
- After converting a pack: re-run the audit (tags unchanged, must stay 0 errors), verify no duplicate answers within the stage, tick the pack below and note the commit hash.

## Second sweep — intro register (James, 2026-08-06)

While repairing ANY pack below, also audit its **intro slides** against the
rewritten "Who the learner is" section of `AGENTS.md`: name the real
grammatical category in titles and bodies (*Mianownik liczby mnogiej*, not
"More than one"; *Biernik = mianownik*, not "subject and object look the
same"), delete cute framing ("A free gift", cheerleading), and break up
walltext. James's ruling: RUPL's learner is linguistically inclined and
learning a hard language on purpose — the English may be sophisticated even
though his Polish is at zero. Worked example, done by hand 2026-08-06:
`a2_plural_nom` slides 0 and 5. This applies to packs NOT otherwise in the
queue too — when a run has spare capacity, sweep the intros of the packs
nearest the learner's current position first (A2 before B2).

## Queue (routine: process up to 3 packs per run, BEFORE building new units)

- [x] `a1_hello` (data/grammar/blocks/a1_hello.json) — 2 item(s): #9 „To jest mój dom.”; #10 „To jest twoja książka.”
- [x] `a2_dat_chunks` (data/grammar/blocks/a2_dat_chunks.json) — 7 item(s): #3 „Podoba mi się ten film.”; #4 „Podoba mi się ta książka.”; #5 „Podoba mi się to miasto.”; #6 „Nie podoba mi się ten film.”; #7 „Smakuje mi ta kawa.”; #8 „Smakuje mi ta zupa.”; #9 „Smakuje mi ten chleb.”
- [x] `a2_directions_func` (data/grammar/blocks/a2_directions_func.json) — 3 item(s): #0 „Przepraszam, gdzie jest apteka?”; #6 „Proszę iść w lewo.”; #7 „Proszę iść w prawo.”
- [x] `a2_imperative` (data/grammar/blocks/a2_imperative.json) — 1 item(s): #8 „Proszę mówić po polsku.” — done `af6fb0a`
- [x] `a2_ordering_func` (data/grammar/blocks/a2_ordering_func.json) — 3 item(s): #0 „Czy jest wolny stolik?”; #1 „Czy ten stolik jest wolny?”; #11 „Czy mogę płacić kartą?” — done `455a593`
- [x] `a2_phone_func` (data/grammar/blocks/a2_phone_func.json) — 4 item(s): #2 „Czy mogę rozmawiać z mamą?”; #3 „Czy mogę rozmawiać z tatą?”; #4 „Czy mogę rozmawiać z siostrą?”; #5 „Nie mogę teraz rozmawiać.” — done `aafaa2e`
- [x] `a2_shopping_func` (data/grammar/blocks/a2_shopping_func.json) — 1 item(s): #8 „Czy mogę płacić kartą?” — done `77a70f1`
- [x] `a2_smalltalk` (data/grammar/blocks/a2_smalltalk.json) — DONE by hand 2026-08-06, items 8-11 incl. the two 3-word composed questions the sweep missed.
- [x] `a2_wrapup_func` (data/grammar/blocks/a2_wrapup_func.json) — 4 item(s): #2 „Czy jest wolny stolik?”; #3 „Czy mogę rozmawiać z siostrą?”; #4 „Jak było w weekend?”; #5 „Proszę iść w lewo.” — done `08832e0`
- [x] `b1_polite` (data/grammar/blocks/b1_polite.json) — done `ea5fa70` — 8 item(s): #0 „Czy mógłby pan mi pomóc?”; #1 „Czy mogłaby pani mi pomóc?”; #2 „Czy mógłby pan mówić po angielsku?”; #3 „Czy mogłaby pani mówić po polsku?”; #4 „Czy mogłaby pani zrobić kawę?”; #5 „Chciałbym zapytać, gdzie jest bank.”; #6 „Chciałabym zapytać, gdzie jest apteka.”; #8 „Dziękuję bardzo za kawę.”
- [x] `b1_stories_func` (data/grammar/blocks/b1_stories_func.json) — done `68b9b4b` — 1 item(s): #2 „Nic się nie stało.”
- [x] `b1_vocative_chunks` (data/grammar/blocks/b1_vocative_chunks.json) — done `0d874a9` — 5 item(s): #6 „Mamo, gdzie jest apteka?”; #7 „Tato, dziękuję za prezent!”; #8 „Babciu, jak się masz?”; #10 „Panie Piotrze, przepraszam, czy mógłby pan mi pomóc?”; #11 „Pani Anno, dziękuję bardzo!” — plus #9 „Dziadku, jestem głodny.” caught by the composition sweep
- [x] `b1_wrapup` (data/grammar/blocks/b1_wrapup.json) — done `7a64ac6` — 3 item(s): #1 „Mamy termin w przyszłym tygodniu.”; #5 „Najpierw szedłem do banku.”; #7 „Spóźniłem się do pracy.” — plus #6 „Nagle zgubiłem się.” caught by the composition sweep
- [x] `b2_gdyby` (data/grammar/blocks/b2_gdyby.json) — done `b0ae11d` — 10 item(s): #0 „Gdybym miał pieniądze, kupiłbym samochód.”; #1 „Gdyby mama miała czas, zrobiłaby obiad.”; #2 „Gdybyś miał czas, zrobiłbyś obiad.”; #3 „Gdyby tata miał czas, zrobiłby kawę.”; #4 „Gdybym miał czas, chciałbym być w parku.”; #5 „Gdyby mama była w sklepie, kupiłaby chleb.”; #6 „Gdyby tata był w domu, mógłby pomóc.”; #7 „Gdybym był w Polsce, chciałbym mówić po polsku.”; #8 „Gdyby mama miała pieniądze, kupiłaby dom.”; #9 „Gdybyś miał pieniądze, chciałbyś nowy samochód.”
- [x] `b2_indirect_q` (data/grammar/blocks/b2_indirect_q.json) — done `d5e29f5` — 10 item(s): #0 „Nie wiem, gdzie jest apteka.”; #1 „Nie wiem, czy sklep jest otwarty.”; #2 „Wiem, kiedy mama będzie w domu.”; #3 „Nie wiem, ile to kosztuje.”; #4 „Wiem, dlaczego tata jest zmęczony.”; #5 „Nie wiem, który film jest dobry.”; #6 „Nie wiem, kto to jest.”; #7 „Nie wiem, co tata robi.”; #8 „Nie wiem, z kim tata pracuje.”; #9 „Nie wiem, czy mama ma skierowanie.”
- [x] `b2_jesli` (data/grammar/blocks/b2_jesli.json) — done `caf2e27` — 10 item(s): #0 „Jeśli masz czas, zrobię obiad.”; #1 „Jeśli sklep jest otwarty, kupię chleb.”; #2 „Jeśli pogoda będzie ładna, będę w parku.”; #3 „Jeśli będzie zimno, będę w domu.”; #4 „Jeśli jesteś zmęczony, idź do domu.”; #5 „Jeśli masz pieniądze, kup bilet.”; #6 „Jeśli tata będzie w domu, przyjdę.”; #7 „Jeśli będziesz głodny, zrobię zupę.”; #8 „Jeśli mama będzie w pracy, tata zrobi obiad.”; #9 „Jeśli to jest dobra decyzja, będzie dobrze.”
- [ ] `b2_ktory_full` (data/grammar/blocks/b2_ktory_full.json) — 10 item(s): #0 „To jest film, o którym mówiłem.”; #1 „To jest książka, o której mówiłem.”; #2 „To jest kolega, z którym pracuję.”; #3 „To jest koleżanka, z którą pracuję.”; #4 „To jest dom, w którym mieszkam.”; #5 „To jest pies, którego mam.”; #6 „To jest brat, którego znasz.”; #7 „To jest autobus, którym jadę.”; #8 „To jest praca, o której mówiłem.”; #9 „To jest sklep, w którym kupuję chleb.”
- [ ] `b2_reported` (data/grammar/blocks/b2_reported.json) — 10 item(s): #0 „Tata powiedział, że sklep jest zamknięty.”; #1 „Mama powiedziała, że kawa jest dobra.”; #2 „Lekarz powiedział, że wyniki są dobre.”; #3 „Powiedziałem, że jestem zmęczony.”; #4 „Powiedziałam, że jestem zmęczona.”; #5 „Tata powiedział, że mama będzie w domu.”; #6 „Siostra powiedziała, że ma nowy samochód.”; #7 „Mama powiedziała, że pogoda będzie ładna.”; #8 „Lekarz powiedział, że mam skierowanie.”; #9 „Brat powiedział, że pracuje w biurze.”
- [ ] `b2_ze_clauses` (data/grammar/blocks/b2_ze_clauses.json) — 10 item(s): #0 „Wiem, że mama jest w domu.”; #1 „Myślę, że kawa jest dobra.”; #2 „Wiem, że tata pracuje.”; #3 „Myślę, że sklep jest zamknięty.”; #4 „Mama mówi, że pogoda jest ładna.”; #5 „Wiem, że brat jest zmęczony.”; #6 „Myślę, że to jest dobre.”; #7 „Wiem, że mam skierowanie.”; #8 „Myślę, że wyniki są dobre.”; #9 „Idę do sklepu, żeby kupić chleb.”

_19 packs · 104 items · generated 2026-08-06 by sweep (answers with ≥4 words, no existing gap fields)._

## Conversion log

- **2026-08-06, cloud routine (fifth repair run):** `b2_gdyby` (10), `b2_indirect_q` (10), `b2_jesli` (10) — **30 items converted**, and the first run where three ENTIRE Pisanie stages convert: every item in all three packs was a composed 4-8 word sentence, so no composition sweep was needed (nothing was under the word cap to re-test). Audit 0 errors (warns 2, both the pre-existing empty-teaches review nodes `a2_prep_review` and `b1_two_futures`), no duplicate answers within any stage — checked with a Python mirror of the engine's own `norm()` rather than by eye, since `norm()` lowercases and strips punctuation and so collapses pairs a naive string compare would miss — and every frame verified by assertion to reconstruct its original sentence character-for-character. `prompt_en` and `explain` were preserved byte-for-byte on all 30 items, and every field outside `type_items` proven identical to HEAD by dict comparison. **Only ONE blank in 30 falls off its unit's teaching point** (`b2_indirect_q` #9, logged per-item): that pack has two `czy` sentences, `czy` is produced at the slide-2 headline item #1, and a second `czy` answer would duplicate within the stage — the same forced choice as `a2_phone_func` #2-#4 and `b1_polite` #3.

  **A convention conflict James should rule on once, because the course is currently split down the middle.** The `accepts` field for cloze items is inconsistent across the repaired packs. James's own hand-conversion (`a2_smalltalk` #8-#11) and the first repair run (`a1_hello`, `a2_dat_chunks`) list the **exact answer only** — `accepts: ["robiłeś"]`. The third and fourth runs (`b1_polite`, `b1_wrapup`) added **ASCII-folded variants** — `accepts: ["mógłby", "moglby"]`, `["szedłem", "szedlem"]`. This is not cosmetic: `norm()` in `practice-grammar.js` lowercases and strips punctuation but does **not** fold diacritics, so today `moglby` is graded correct in `b1_polite` and `robilas` is graded wrong in `a2_smalltalk` — and the cloze stage prints **„Wpisz brakujące słowo · diakrytyki ważne"** on screen while doing it. These 30 items take the **conservative branch: exact answer only**, following James's own conversion and keeping the app's on-screen promise honest. Capitalisation needs no variant at all — `norm()` lowercases both sides, so the sentence-initial answers (`Gdybym`, `Gdybyś`, `Gdyby`, `Jeśli`) accept either case for free, and the earlier runs' `["Chciałabym", "chciałabym", ...]` lists were redundant rather than wrong. **If James wants the lenient reading, the fix is one sweep over all repaired packs plus a change to that on-screen line — not more per-pack drift.**

  **Two judgment calls to smoke.** (1) `b2_gdyby` #6 now asks Dad to type `mógłby`, which sits under `b1_polite`'s unresolved MÓGŁBY QUARANTINE. It was kept rather than dodged, for two reasons: the item previously demanded the *whole* sentence typed, so this strictly **reduces** what is produced and adds no new exposure; and `b1_polite` #0/#1 already ship `mógłby`/`mogłaby` as blanks from the third repair run, so moving this one to `pomóc` would make the course inconsistent with itself rather than more conservative. If the quarantine is tightened at smoke, both packs move together. (2) `b2_jesli` #6's frame is `Jeśli tata będzie w domu, ___.` — the result clause is a single word (`przyjdę`), so the input sits at the very end with nothing after it but the full stop. It renders correctly, but it is the first cloze in the course whose frame ends at the blank, and it is worth a look in the hand.

  Remaining: **3 packs, 30 items — all `b2_*`** (`b2_ktory_full`, `b2_reported`, `b2_ze_clauses`).

- **2026-08-06, cloud routine (fourth repair run):** `b1_stories_func` (1), `b1_vocative_chunks` (5+1), `b1_wrapup` (3+2 — see below) — **11 items converted**, 9 from the queue list and 2 caught by the refinement's composition sweep (`b1_vocative_chunks` #9 „Dziadku, jestem głodny.”, `b1_wrapup` #6 „Nagle zgubiłem się.” — both 3 words, both connective/calling-chunk + bolted-on clause). Audit 0 errors (warns 2, both the pre-existing empty-teaches review nodes `a2_prep_review` and `b1_two_futures`), no duplicate answers within any Pisanie stage, non-`type_items` fields diffed field-by-field against HEAD to prove tags unchanged, and every frame verified by assertion to reconstruct its original sentence character-for-character. **`b1_vocative_chunks` is the first pack where the teaching point was unavailable on EVERY converted item**, not just one or two: the six calling-forms are each produced whole at items 0-5, so any vocative blank would have duplicated an answer within the stage. All six blanks therefore fall on the load-bearing form of the bolted-on clause, logged per-item under the fallback clause, with the calling-form left visible in the frame so the chunk is still read whole — and no new explain states a vocative endings rule (spine decision #4 holds). **Three judgment calls for James:** (1) `b1_vocative_chunks` #10 takes the CONSERVATIVE fork against the consistency rule — it blanks `pomóc`, not `mógłby`, so the course adds no third production of a form under b1_polite's unresolved MÓGŁBY QUARANTINE; if you clear the quarantine at smoke, move it to `mógłby` to match its source. (2) The line drawn on the composition sweep, applied consistently and worth ruling on once: **a bolted-on part makes a sentence composed** (vocative/connective + clause → cloze), while **a single minimal clause with no bolted-on part is a pattern unit** and stays typed whole — so `b1_wrapup` #0 „Planuję spotkanie." (verb + object) and #2 „Muszę to załatwić." (modal + infinitive) were HELD, following the previous run's precedent of holding borderline ≤3-word items rather than over-converting. (3) `b1_wrapup` #1 blanks the time chunk WHOLE (`w przyszłym tygodniu`) because `check_new.py` reports `tygodniu` is not independently taught — b1_plans owns the phrase as one multi-word lemma, so blanking inside it would have demanded an untaught form. Note the capstone consistency rule could NOT be applied literally in `b1_wrapup`: its three source units (b1_plans, b1_stories, b1_journeys) are vocab packs with no Pisanie stage, so no source frame existed to mirror. Remaining: **6 packs, 60 items — all `b2_*`.**
- **2026-08-06, cloud routine (third repair run):** `a2_shopping_func` (1), `a2_wrapup_func` (4), `b1_polite` (8) — 13 items converted, audit 0 errors (warns 2, both pre-existing empty-teaches review nodes), no duplicate answers within any Pisanie stage, and every frame verified character-for-character to reconstruct its original sentence. **Consistency rule applied for the first time and worth keeping:** `a2_wrapup_func` is a capstone whose items are verbatim copies of phrases from five source units, so each converted item took its **source unit's own frame and blank** (stolik ← a2_ordering_func #0, siostrą ← a2_phone_func #4, w weekend ← a2_smalltalk #8, lewo ← a2_directions_func #6) rather than a fresh choice — the capstone should drill a phrase the way it was taught. Only one blank landed off the teaching point: `b1_polite` #3 (*polsku*), because mogłaby was spent at #1 and mówić at #2; logged in the item note. **One judgment call for James to smoke:** `b1_polite` #0/#1 now ask Dad to type `mógłby`/`mogłaby` into the frame. That pack carries a MÓGŁBY QUARANTINE whose last line reads "the learner produces nothing but the frozen frames." It was checked, not assumed: the fence bans *paradigming* móc (no mógłbym/mogłabyś, no plural, no derivation from mogę, no four-way ending drill), the whole opening stays on screen, and the queue rule James locked names `mógłby` itself as an example blank target — so the two were treated as compatible. If it feels wrong in the hand, the conservative fix is to move both blanks onto `pomóc`/`pani` and leave the quarantined forms permanently unproduced. Remaining: 9 packs, 69 items.
- **2026-08-06, cloud routine (second repair run):** `a2_imperative` (1), `a2_ordering_func` (3), `a2_phone_func` (4) — 8 items converted, audit 0 errors (warns 2, both pre-existing empty-teaches review nodes), no duplicate answers within any Pisanie stage. Every blank landed on a teaching point except `a2_phone_func` #2-#4, where the no-duplicates rule forced the choice: three items share the frame `Czy mogę rozmawiać z ___?` and blank the person (mamą/tatą/siostrą) rather than the infinitive, which would have repeated `rozmawiać` three times in one stage; #5 carries the infinitive blank instead, so the new word is still produced once. Logged per-item in item `note`s. **Two judgment calls for James to smoke:** `a2_ordering_func` "Co pan poleca? / Co pani poleca?" are conjugated in form but the pack teaches `polecać` as an opaque chunk that is never conjugated anywhere, so they were held as frozen chunks and stay typed whole — if they feel like sentences in the hand, they should become `Co ___ poleca?` (pan/pani). Same call on "Było bardzo smaczne.", held as a pattern phrase on the "Było super." precedent. Remaining: 12 packs, 82 items (recounted from the table rows — the previous entry's "16 packs, 92 items" counted packs loosely; item arithmetic is unchanged: 104 swept − 22 converted = 82).
- **2026-08-06, cloud routine (B2 batch 10):** `a1_hello` (2), `a2_dat_chunks` (7), `a2_directions_func` (3) — 12 items converted, audit 0 errors, no duplicate answers within any Pisanie stage. Two blanks did NOT land on the unit's teaching point and are logged per-item in an item `note`, per the queue's own fallback clause: `a1_hello` #9/#10 blank the possessive rather than `jest`, because items 2 and 6 already type `jest` whole and a third identical answer would break the no-duplicates rule. Everywhere else the blank is the teaching point: `a2_dat_chunks` blanks the plain-form noun (the subject-flip the unit exists for) and the front `Nie`; `a2_directions_func` blanks the unchanged place name and the direction word its own explain says is the only part that moves. Remaining: 16 packs, 92 items.
